name: Python Docker CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Tests
        run: echo "No tests yet"

      - name: Build Docker Image
        run: docker build -t python-app .

      - name: Deploy to Ubuntu Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd python-docker-cicd || git clone https://github.com/UlSaSandeep949/python-docker-cicd.git
            cd python-docker-cicd
            git pull origin main
            docker stop python-app || true
            docker rm python-app || true
            docker build -t python-app .
            docker run -d -p 5000:5000 --name python-app python-app